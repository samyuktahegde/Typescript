let todo: string = "Pick up dry cleaning";
let name: string = "Jess";

// var container = document.getElementById('container');
for (var x = 0; x <= 5; x++) {
    const counter = x;
}
console.log(counter);

var displayName = `Todo #${todo.id}`